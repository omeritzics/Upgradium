name: updatium
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  # Upgraded SDK to support modern packages and Null Safety
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Core dependencies aligned to compatible versions
  easy_localization: ^3.0.7
  easy_localization_loader: ^2.0.2
  connectivity_plus: ^6.0.3
  battery_plus: ^6.2.2
  flutter_foreground_task: ^9.1.0
  http: ^1.4.0
  shared_preferences: ^2.3.5
  url_launcher: ^6.3.1
  package_info_plus: ^8.1.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# This section is the "safety net" that solves the conflict you encountered
dependency_overrides:
  web: ^1.0.0
  connectivity_plus: ^6.0.3

flutter:
  uses-material-design: true
  # Add your assets/fonts here as they were before
